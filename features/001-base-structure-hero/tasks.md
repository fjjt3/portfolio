# 001 · Base Structure and Hero — Tasks

- [ ] Run the Astro installation wizard in the terminal and select TypeScript (Strict).
- [ ] Install and integrate Tailwind CSS.
- [ ] Build the `Layout.astro` component (add basic meta tags for SEO).
- [ ] Design the `Navbar` component with responsive behavior (collapsible or simplified menu on mobile).
- [ ] Create the `Hero` section using eye-catching color gradients for the main text.
- [ ] Check in the local browser (`localhost:4321`) that styles apply correctly.
- [ ] Validate against the acceptance criteria in `spec.md`.
- [ ] Move the feature to "Done" in `../../constitution/roadmap.md`.
